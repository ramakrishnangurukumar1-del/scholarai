# Minimum versions chosen for Python 3.12+ wheel availability.
fastapi>=0.118
uvicorn[standard]>=0.34
sqlalchemy>=2.0.43
alembic>=1.14
psycopg[binary]>=3.3.1      # PostgreSQL driver
pymysql>=1.1                # MySQL driver
pydantic>=2.12
pydantic-settings>=2.7
python-dotenv>=1.0
python-multipart>=0.0.20
pyjwt>=2.10
email-validator>=2.2

# OCR (needs the Tesseract binary installed separately)
pytesseract>=0.3.13
pillow>=11
pypdf>=5
pymupdf>=1.24        # render scanned PDFs for OCR

# Optional — real LLM assistant (used only if GOOGLE_API_KEY is set)
google-genai>=1.0

# Event streaming (used only if KAFKA_BOOTSTRAP is set)
kafka-python-ng>=2.2

# Tests
pytest>=8
httpx>=0.27
